apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ps5-stock-checker-pvc
spec:
  storageClassName: nfs-storageclass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
